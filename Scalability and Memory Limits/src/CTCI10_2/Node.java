package CTCI10_2;

/**
 * Created by Gautam on 8/21/16.
 */
public class Node
{
    /*TO TRACK THE MOST IMPORTANT NODE TO TRACK A FRIEND WHO HAS A LOT OF FRIENDS

    Node mostImportant = null

    void search(node) {
        nodeInitial = pick a node
        if(!mostImportant) {
            mostImportant = nodeInitial
        }

        for(nodeInitial:outgoingNodes as node) {
            node:visits++ // this is how you track in-edges

            if(node.visits > mostImportant.visits)
                mostImportant = node

            if(node is unvisited) {
                search(node)
                node:visited = true
            }
        }
    }
    */
}
